import React, { use } from 'react';
import { AuthContext } from '../Context/AuthContext';

const Home = () => {

    return (
        <div>
            <h1>Home</h1>
        </div>
    );
};

export default Home;